# Day 12

Day 12 in typst.

> [!NOTE]
> I had to make a change to the typst compiler to do the recursion in
> this... I guess since it's a typsetting program and not a programming
> language, 80 levels of recursion is enough.  I opted to increase that
> to 1000.
>
> It does also use like 15GB of memory while running on the real input,
> so I advise against that.

```sh
$ make
$ mupdf run.pdf
```
